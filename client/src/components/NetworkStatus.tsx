// Offline feature removed — this component is intentionally empty.
// The import in App.tsx is kept to avoid a missing-module error; it renders nothing.
export function NetworkStatus() {
  return null;
}
